package com.backendrumba.rumba_music.model;import jakarta.persistence.*;import lombok.Data;@Entity@Datapublic class Song {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    private String name;  // Nombre de la canción    private String filePath;    @Lob    private byte[] fileData;  // Este campo almacenará los datos binarios del archivo    @ManyToOne  // Relación Many-to-One con User    @JoinColumn(name = "user_id")  // Nombre de la columna en la base de datos    private User user;  // Usuario que subió la canción (DJ o ADMIN)}