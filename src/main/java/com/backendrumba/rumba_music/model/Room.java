package com.backendrumba.rumba_music.model;import com.fasterxml.jackson.annotation.JsonManagedReference;import jakarta.persistence.*;import lombok.Data;import java.util.List;@Entity@Datapublic class Room {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    private String roomCode;  // Código de la sala generado por el DJ    @OneToMany(mappedBy = "room")    @JsonManagedReference  // Para controlar la serialización    private List<User> users;  // Lista de usuarios en la sala    private boolean isPlaying;  // Estado de si la música está en reproducción}