package com.backendrumba.rumba_music.model;import com.fasterxml.jackson.annotation.JsonBackReference;import jakarta.persistence.*;import lombok.Data;@Entity@Datapublic class User {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    private String name;  // Nombre del dispositivo o un identificador    private boolean connected;  // Estado de conexión (true si está conectado)    @Enumerated(EnumType.STRING)  // Guarda el rol como un String en la base de datos    private Roles role;    @ManyToOne  // Un usuario puede estar en una sala    @JoinColumn(name = "room_id")    @JsonBackReference  // Para evitar la serialización recursiva    private Room room;}